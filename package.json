{
  "name": "e_menu_local_business",
  "version": "1.0.15",
  "private": true,
  "type": "module",
  "description": "Offline-first bilingual Cambodian e-menu for static GitHub Pages hosting.",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-project.mjs",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
